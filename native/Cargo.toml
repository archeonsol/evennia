[package]
name = "evennia_native"
version = "0.1.0"
edition = "2021"

[lib]
name = "evennia_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = ">=0.22", features = ["extension-module"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
