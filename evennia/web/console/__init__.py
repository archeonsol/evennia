"""Web surface for the engine console.

The API lives here; the runtime, registry, and services live in
:mod:`evennia.console`. The split follows the existing web layout: this package
is Django plumbing, that one is engine substrate.
"""
